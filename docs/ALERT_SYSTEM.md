# ระบบการแจ้งเตือน (Alert System)

## ภาพรวม
ระบบการแจ้งเตือนอัตโนมัติที่ตรวจสอบสถานะอุปกรณ์และข้อมูลเซ็นเซอร์ แล้วสร้างการแจ้งเตือนเมื่อพบความผิดปกติ

## ฟีเจอร์

### 1. การแจ้งเตือนอัตโนมัติ
- ✅ **แบตเตอรี่ต่ำ**: เมื่อแบตเตอรี่ต่ำกว่า 20%
- ✅ **อุปกรณ์ออฟไลน์**: เมื่อไม่ได้รับข้อมูลมานานกว่า 10 นาที
- ✅ **เกินค่าขีดจำกัด**: อุณหภูมิ/ความชื้นเกินค่าที่กำหนด
  - อุณหภูมิสูง: > 35°C
  - อุณหภูมิต่ำ: < 10°C
  - ความชื้นสูง: > 80%
  - ความชื้นต่ำ: < 20%

### 2. ประเภทการแจ้งเตือน
- `threshold_exceeded` - เกินค่าขีดจำกัด
- `device_offline` - อุปกรณ์ออฟไลน์
- `battery_low` - แบตเตอรี่ต่ำ
- `error` - ข้อผิดพลาด
- `connection_lost` - การเชื่อมต่อขาดหาย
- `sensor_error` - เซ็นเซอร์ผิดพลาด
- `maintenance_required` - ต้องการบำรุงรักษา
- `custom` - กำหนดเอง

### 3. ระดับความสำคัญ
- `critical` - วิกฤต (สีแดง)
- `warning` - คำเตือน (สีเหลือง)
- `info` - ข้อมูล (สีน้ำเงิน)

## วิธีใช้งาน

### การเริ่มต้น Alert Monitor Service

```bash
# รัน alert monitor service
node scripts/alert_monitor.cjs
```

Service นี้จะ:
- ตรวจสอบสถานะอุปกรณ์ทุก 2 นาที
- ตรวจสอบข้อมูลเซ็นเซอร์ทุก 1 นาที
- ป้องกันการแจ้งเตือนซ้ำภายใน 30 นาที

### การสร้างการแจ้งเตือนด้วยตนเอง

```bash
# ผ่าน API
POST /api/alerts
Content-Type: application/json

{
  "device_id": "uuid-here",
  "type": "custom",
  "severity": "warning",
  "message": "ข้อความแจ้งเตือน"
}
```

### การสร้างการแจ้งเตือนอัตโนมัติ (สำหรับ MQTT/System)

```bash
POST /api/alerts/auto
Content-Type: application/json

{
  "device_id": "uuid-here",
  "type": "threshold_exceeded",
  "severity": "warning",
  "message": "อุณหภูมิสูงเกินไป"
}
```

## การปรับแต่งค่า Threshold

แก้ไขไฟล์ `scripts/alert_monitor.cjs`:

```javascript
const THRESHOLDS = {
  battery_low: 20,           // แบตเตอรี่ต่ำกว่า 20%
  offline_minutes: 10,        // ออฟไลน์เกิน 10 นาที
  temperature_high: 35,       // อุณหภูมิสูงกว่า 35°C
  temperature_low: 10,        // อุณหภูมิต่ำกว่า 10°C
  humidity_high: 80,          // ความชื้นสูงกว่า 80%
  humidity_low: 20,           // ความชื้นต่ำกว่า 20%
}
```

## การใช้งานในหน้าเว็บ

### 1. ดูการแจ้งเตือนทั้งหมด
- ไปที่ `/admin/alerts`
- กรองตามประเภท, ระดับความสำคัญ, หรือสถานะการอ่าน
- Auto-refresh ทุก 30 วินาที

### 2. จัดการการแจ้งเตือน
- ✓ ทำเครื่องหมายว่าอ่านแล้ว
- ✗ ลบการแจ้งเตือน
- 🔄 รีเฟรชด้วยตนเอง
- 🔔 เปิด/ปิด auto-refresh

### 3. Realtime Updates
ระบบใช้ Supabase Realtime เพื่ออัพเดทการแจ้งเตือนแบบทันที ไม่ต้องรีเฟรชหน้า

## API Endpoints

### GET /api/alerts
ดึงรายการการแจ้งเตือน
- Query params: `deviceId`, `limit`
- Response: รายการ alerts พร้อมข้อมูล device

### POST /api/alerts
สร้างการแจ้งเตือนใหม่ (ต้อง verify device ownership)

### POST /api/alerts/auto
สร้างการแจ้งเตือนอัตโนมัติ (ไม่ต้อง auth, ใช้สำหรับ system)

### PATCH /api/alerts/[id]
อัพเดทสถานะการอ่าน

### DELETE /api/alerts/[id]
ลบการแจ้งเตือน

## การ Deploy

### Development
```bash
# Terminal 1: Next.js dev server
npm run dev

# Terminal 2: Alert Monitor Service
node scripts/alert_monitor.cjs
```

### Production
```bash
# ใช้ PM2 หรือ systemd เพื่อรัน alert monitor service
pm2 start scripts/alert_monitor.cjs --name alert-monitor

# หรือใช้ Docker
docker-compose up -d
```

## การเพิ่มประเภทการแจ้งเตือนใหม่

1. เพิ่มประเภทใน `alert_monitor.cjs`:
```javascript
async function checkNewCondition() {
  // Logic ตรวจสอบของคุณ
  await createAlert(device, 'new_type', 'warning', 'ข้อความ')
}
```

2. เพิ่มใน filter ของหน้า alerts:
```tsx
<SelectItem value="new_type">ชื่อประเภทใหม่</SelectItem>
```

3. เพิ่มการแสดงผล:
```tsx
{alert.type === "new_type" && "ชื่อประเภทใหม่"}
```

## Troubleshooting

### การแจ้งเตือนไม่ถูกสร้าง
1. ตรวจสอบว่า alert monitor service รันอยู่
2. ตรวจสอบ threshold values
3. ดู console logs

### การแจ้งเตือนซ้ำ
- ระบบป้องกันการแจ้งเตือนซ้ำภายใน 30 นาที
- ถ้าต้องการปรับเวลา แก้ไขใน `createAlert()` function

### Realtime ไม่ทำงาน
1. ตรวจสอบ Supabase Realtime enabled
2. ตรวจสอบ RLS policies
3. ตรวจสอบ browser console

## Performance

- Alert Monitor Service ใช้ memory ประมาณ 50-100 MB
- Database queries มี indexes สำหรับ performance
- Realtime subscriptions มีการจัดการ cleanup อัตโนมัติ

## Security

- ✅ User-level filtering (ดูเฉพาะ alerts ของตนเอง)
- ✅ Device ownership verification
- ✅ RLS policies enabled
- ✅ Service role key สำหรับ monitor service เท่านั้น

# Supabase (required)
NEXT_PUBLIC_SUPABASE_URL=your_supabase_url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key
# Server-only: keep this out of the browser and set it in your deployment secrets
SUPABASE_SERVICE_ROLE_KEY=your_service_role_key

# MQTT (used by the browser client)
# Use a secure WebSocket URL for browser connections (wss://). Example: "wss://broker.hivemq.com:8884/mqtt"
NEXT_PUBLIC_MQTT_BROKER=wss://broker.hivemq.com:8884/mqtt
NEXT_PUBLIC_MQTT_USERNAME=
NEXT_PUBLIC_MQTT_PASSWORD=

# Optional client ID prefix and topic prefix
NEXT_PUBLIC_MQTT_CLIENT_ID_PREFIX=iot-client-
NEXT_PUBLIC_MQTT_TOPIC_PREFIX=iot/

# Secret used by server-side publishers/subscribers to sign HTTP ingest requests.
# Keep this secret out of the browser and set it in your deployment secrets.
MQTT_INGEST_SECRET=your_long_random_secret_here

# Add any other env vars from SETUP.md or project docs
# NEXT_PUBLIC_SUPABASE_... etc
